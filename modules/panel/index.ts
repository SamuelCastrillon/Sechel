// Panel module stub (Slice 4). Intentionally empty: the real admin UI, server
// actions, and session auth land in later slices. NO server-only here so client
// components can safely import this barrel; guards go in panel/actions and
// panel/auth when those are implemented.
export {};
